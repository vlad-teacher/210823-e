import React from "react";
import ReactDOM from "react-dom/client";
import { App } from "./App";

// TODO compount component MODAL
// TODO focus input after OK btn click
const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <App/>
);
